import { FC } from "react";

const UserInformation: FC<any> = (currentUser) => {
  const user = currentUser.currentUser;

  return (
    <div>
      <h1>Nothing Here</h1>
    </div>
  );
};

export default UserInformation;
